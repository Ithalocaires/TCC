import React from 'react';
import { View, Text, TouchableOpacity, Image, StyleSheet } from 'react-native'

const WaitRoom = ({ navigation }) => {
    return (
        <Text>TESTE</Text>
    );
}

export default WaitRoom;